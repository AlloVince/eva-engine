<?
$title = 'Groups';
$this->headTitle($title, 'SET');
$user = $this->user;
$item = $this->item;
?>

<div class="container">
    <header class="group-header">
        <?=$this->widget('Epic', 'widgets/groupnav', $this->vars())?> 
    </header>
</div>

<div class="container">
    <div class="page-bg">
        <div class="row">

            <div class="span8">
                <div class="group-main">
                    <?=$this->widget('Epic', 'widgets/groupcard', $this->vars())?>  
                        <?
                        $formAction = '';
                        if(!$this->form){
                            $formAction = $this->post['id'] ? $this->uri('/events/edit/') : $this->uri('/group/events/');
                        }
                        ?>
                        <?=$this->widget('Epic', 'epic/event/form', array(
                            'form' => $this->form,
                            'formAction' => $formAction,
                            'item' => $this->event,
                            'callbackInput' => '<input type="hidden" name="callback" value="' . '/group/' . $item['groupKey'] . '/sendmail/?type=create&event=' . '">',
                            'formExt' => '<input type="hidden" name="group_id" value="' . $this->item['id'] . '">'
                        ))?>
                </div><!--group main end-->
            </div><!--page main left end-->
            <div class="span4">
                <?=$this->widget('Epic', 'widgets/groupside', $this->vars())?>  
            </div>

        </div><!--page main right end-->
    </div><!-- page-bg end-->
</div><!--container end-->
