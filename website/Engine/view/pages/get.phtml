<?
$post = $this->post;
$title = $post['title'];
$this->headTitle($title, 'SET');
?>
<div class="container">
	<header class="page-header">
		<h1><?=$this->escapeHtml($title); ?></h1>
	</header>
	<div class="typo"><?=$post['Text']['contentHtml']?></div>
</div>
