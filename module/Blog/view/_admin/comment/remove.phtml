<?
$item = $this->item;
$title = $this->_('Delete Comment');
$this->headTitle($title, 'SET');
$form = new Blog\Form\CommentDeleteForm();
$form->setView($this)
->setMethod('delete')
->setAction($this->uri('/admin/blog/comment'))
->bind($item)
->prepare();
?>

<?if($item):?>
<div class="alert alert-block">
   <h2 class="alert-heading"><?=$title?></h2>
   <p><?=$this->_(array('Comment %id% and all children categories will be deleted, are you sure?', $item['id']))?></p>
   <p>
		<form class="form-horizontal" <?=$this->formAttr($form)?>>
			<?=$form->restful();?>
			<?=$this->formInput($form->get('id'));?>
            <button class="btn btn-danger" type="submit"><?=$this->_('Delete')?></button>
            <input name="callback" type="hidden" value="<?=$this->callback?>" />
            <a class="btn" href="<?=$this->callback?>"><?=$this->_('Go Back')?></a>
   		</form>
	</p>
</div>
<?else:?>
<div class="alert alert-success container">
    <h2 class="alert-heading"><?=$this->_('Comment have been deleted')?></h2>
</div>
<?endif?>
